[project]
name = "automancer"
version = "0.1.0"
description = "A cellular automata simulator."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy >= 2.4.2",
    "matplotlib >= 3.10.8",
]
